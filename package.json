{
  "name": "chatwoot-ai-gateway",
  "version": "0.1.0-bk",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:local": "wrangler dev --config=wrangler.dev.toml --local --port=8787",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "verify": "bash scripts/verify-all.sh",
    "smoke": "node scripts/smoke-test.mjs"
  }
}
